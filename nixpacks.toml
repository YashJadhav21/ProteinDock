[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "nodejs_20", "curl"]

[phases.install]
cmds = [
  "python3 --version",
  "pip --version",
  "pip install --no-cache-dir biopython numpy rdkit meeko openbabel-wheel",
  "python3 -c 'import Bio; import numpy; import rdkit; print(\"✅ All Python packages installed\")'",
  "chmod +x install.sh",
  "bash install.sh"
]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "cd backend && node server.js"
