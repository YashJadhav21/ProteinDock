[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "nodejs_20", "curl", "stdenv.cc.cc.lib"]

[start]
cmd = "export LD_LIBRARY_PATH=$(find /nix/store -name 'libstdc++.so.6' 2>/dev/null | head -n1 | xargs dirname):$LD_LIBRARY_PATH && cd backend && node server.js"
